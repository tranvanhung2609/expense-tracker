import React from 'react';

/**
 * BiometricLockOverlay (Deprecated & Disabled)
 * Passing through children directly.
 */
export default function BiometricLockOverlay({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
